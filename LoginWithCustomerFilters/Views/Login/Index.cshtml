@model WebAppStorePractice.Models.UserModel
@{
    ViewData["Title"] = "Log in";
    Layout = "/Views/Login/_CustomAuthenticationLayout.cshtml";
}

<h4>Sign in to One Stop Shop</h4>
<hr />
@*<div class="row">
    <div class="col-md-4">*@
        <form asp-action="ProcessLogin">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="UserName" class="control-label"></label>
                <input asp-for="UserName" placeholder="User Name" class="form-control" />
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Password" class="control-label"></label>
                <input asp-for="Password" placeholder="Password" class="form-control" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <input type="submit" value="Login"  class="btn btn-primary rounded-pill w-100 mt-4" />
            </div>
        </form>
           @* <div>
                <h2>New Customer?</h2>
                <h5>Register with One Stop Shop today</h5>
                <div>
                    <a asp-controller="Login" asp-action="Register" type="button" class="btn btn-primary rounded-pill">Register</a>
                </div>
            </div>*@
        
  @*  </div>
</div>
*@
<div>
    <a asp-controller="Product" class="btn btn-outline-light mt-3 rounded-pill" asp-action="Index">Continue Shopping</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
