@model IEnumerable<ListAndSaveProducts.Models.ProductModel>
@{
    ViewData["Title"] = "Home Page";
   
}

@Html.Partial("_ShowDetailsModal")
<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <h1 class="text-primary">Latest Offers</h1>

    @*<div class="container d-flex flex-wrap">

        @foreach (var item in Model)
        {
            <div id="card-number-@item.Id">

                <partial name="_HomeProductCard" model="item" />


            </div>
        }
    </div>*@
    
</div>
<div class="row">
    @foreach (var item in Model.Take(9))
    {   
        <div class="col-md-4">
            <div id="card-number-@item.Id">
                <partial name="_HomeProductCard" model="item" />
            </div>
            <div class="d-flex p-4">
                <div class="row justify-content-between">
                    <div class="col-auto">
                        <i class="fa fa-heart" style="color:red;pointer-events:fill" contextmenu="Added To WishList"></i>
                    </div>
                    <div class="col-auto ml-auto">
                        <button value="@item.Id" type="button" class="bg-transparent border-0" id="showdetailsmodal1" data-toggle="modal" data-target="#showDetailsModal">
                            <i class="fa fa-shopping-cart" style="color:cadetblue"></i>
                        </button>
                    </div>
                    <div class="col-sm-8">
                        <a class="btn btn-default" href="/Product/Index">View More &raquo;</a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
